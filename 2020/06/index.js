console.log(require('fs').readFileSync('./input.txt').toString().split('\n\n').map(e => (e.split('\n'))).map(nested => nested.join()).map(element => element.split('').filter((element, index, arr) => (arr.indexOf(element) === index)).join('').replace(',', '').length).reduce((a, b) => a + b));